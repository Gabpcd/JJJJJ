
!.vscode/extensions.json
# Editor directories and files
# Logs
# TypeScript incremental build artefacts (générés par tsc -b)
*.local
*.log
*.njsproj
*.ntvs*
*.sln
*.suo
*.sw?
*.tsbuildinfo
.DS_Store
.env
.env.local
.env.production
.idea
.vscode/*
/.env.local
/playwright-report
/test-results
dist
dist-ssr
lerna-debug.log*
logs
node_modules
npm-debug.log*
pnpm-debug.log*
supabase/.temp/
yarn-debug.log*
yarn-error.log*
tests/load/results/

# ─── Secrets ANS (mTLS API FHIR Annuaire Santé) ───
# Clé privée + certificat client. Le certificat (.pem) est public mais la clé
# (.key) ne doit JAMAIS quitter la machine de Gabrielle. Le dossier entier est
# exclu pour éviter toute fuite accidentelle. La doc de génération est dans
# docs/ANS-API-FHIR-AUTHENTIFICATION.md.
.secrets-ans/
*.key
# Ne pas wildcard *.pem ici : node_modules / fixtures peuvent légitimement
# contenir des PEM publics (root CAs, certificats de test). Si un .pem
# privé doit être exclu, l'ajouter explicitement.

# ─── Firebase / Push natif (clés sensibles) ───
# Fichiers de config Firebase contenant des clés API. Générés depuis
# Firebase Console, déposés localement par Gabrielle, jamais committés.
# Procédure : docs/PUSH_NATIVE_FINAL.md.
android/app/google-services.json
ios/App/App/GoogleService-Info.plist
**/AuthKey_*.p8

# Worktrees temporaires créées par les agents (jamais committées).
.claude/worktrees/

# Exports prospection (PII) — ne pas versionner les CSV
exports/prospection/*.csv
